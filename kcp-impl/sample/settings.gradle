pluginManagement {
    repositories {
        mavenLocal()
        maven { url 'https://mirrors.tencent.com/nexus/repository/maven-public/' }
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositories {
        mavenLocal()
        maven { url 'https://mirrors.tencent.com/nexus/repository/maven-public/' }
    }
}